# MVP 加固清单

> 目的：把当前工程评审结论收口成一份可执行清单，避免后续继续在“是否已经够 MVP”上反复讨论。

> 当前判断：`Next.js App Router + TypeScript` 是产品主线。当前架构对 MVP 仍然成立，不需要推翻重做；但在从本机演示走向外部试用前，必须补几项工程加固。

> 2026-03-25 约定：这份文件是当前“工程评审 + 设计评审”后续动作的唯一待办清单。
> `task_plan.md`、`progress.md`、`findings.md`、`current-handoff.md` 只保留摘要、阶段判断和验证记录，不再各自维护平行 TODO。
> 如果后续评审再产生新动作，先同步到这里，再更新其它文档。
> 除非确实需要独立的设计 / 架构 / 对外说明文档，否则不要再新建 docs 文件来单独记任务。
> 所有新需求只收口到本清单，按优先级执行；紧急修复永远优先。

## 1. 当前结论

当前架构仍然适合 MVP，前提是严格遵守以下边界：

- 单仓单体应用，不拆微服务
- 当前主链路仍以规则 / 状态机驱动为主
- LLM 目前只作为可选增强，不作为主流程硬依赖
- 本地文件存储只用于本机演示，不用于外部可部署环境
- 新增产品能力默认继续落在 `Next.js` 主线，不继续让 `index.html` 和主应用隐性分叉

## 2. 必须现在做

这些事项不做，MVP 的工程边界会不清楚，后面容易反复返工。

### 2.1 明确部署边界

- [x] 在部署说明、README、环境配置说明里明确写死：
  - 本机演示可用本地文件存储
  - 外部试用 / 预览部署必须使用 Postgres
- [x] 不再把 `/tmp/ai-quality-demo-store.json` 描述成可对外试用方案
- [x] 为部署环境准备一份最小环境变量清单：
  - `DATABASE_URL`
  - 可选的 `AI_QUALITY_LLM_*` 配置

### 2.2 固定 LLM 接入边界

- [x] 约定后续所有在线模型调用只能经由 `lib/server/llm.ts`
- [x] 不允许在：
  - `workflow-engine.ts`
  - `guided-thinking.ts`
  - `report-builder.ts`
  - `components/workspace.tsx`
  里直接写服务接入 / 模型 / 接口地址逻辑
- [x] 如果后续要接 `copilot` 或 `report` 级 LLM，优先先补统一入口 / 路由层边界，再补能力

### 2.3 建立离线原型回灌账本

- [x] 写一份 `index.html -> Next.js` 的迁移清单
- [x] 至少记账这些尚未完全回灌的能力：
  - 项目重命名 / 删除
  - 侧栏折叠态
  - Word / PDF 导出入口
  - D1 固定团队成员
  - D2 中文 5W1H 样式
  - 品牌呈现差异
- [x] 迁移账本已落在 [`index-html-to-nextjs-migration-ledger.md`](./index-html-to-nextjs-migration-ledger.md)，后续按账本持续更新
- [x] 没进账本的离线原型新功能，默认不再新增

### 2.4 把运行态污染写入团队操作常识

- [x] 保持 `npm run dev` 使用 polling，不随手改回裸 `next dev`
- [x] 不在同一工作目录混跑 `next dev` 和 `next start`
- [x] 如果需要生产验证，先 `npm run build`，再单独起 `next start`
- [x] 先查端口再起服务：
  - `3001` 作为默认应用端口
  - 并行实例顺延到 `3002+`

### 2.5 第一轮工作台界面收口

- [x] 调整移动端信息顺序
  - 确保“当前案件 / 当前阶段 / 当前目标”优先于案件列表
- [x] 去重重复状态层
  - 收敛顶部状态标签、摘要条、阶段卡片、`copilot brief` 的重复表达
- [x] 抬高 `copilot brief` 与下一步动作的视觉优先级
  - 让页面先回答“现在先补什么”，再展示次级状态信息
- [x] 把报告工具入口抬到更容易发现的位置
  - 避免预览 / HTML 报告入口埋得过深
- [x] 统一品牌与文案
  - 收口 `Pin2Pin / 芯科元析 / 首单引导 / 种子案例` 的混用
- [x] 修正过小触控目标
  - 优先处理 `新建`、`查看全部阶段` 等高频点击区域
- [x] 首单引导与空状态补齐
  - 明确告诉用户第一步做什么、什么时候能看到第一版结果
  - 空状态下给出清晰的“下一步”提示

## 3. 发布前必须做

这些不一定今天就做，但在对外试用或 MVP 发布前必须补上。

### 3.1 真实浏览器冒烟验证

- [x] 增加一条最小真实浏览器冒烟流程
- [x] 补第二条已有案件续推流程
- [x] 覆盖路径：
  - 打开首页
  - 新建空白案件或载入种子案例
  - 输入一条证据
  - 生成报告预览
- [x] 当前 smoke 已覆盖两条路径：
  - 首单空白案件 -> evidence -> correction -> summary -> 分析结论预览
  - 已有种子案件 -> 继续补证据 -> 行动方案预览
- [x] 验证项：
  - 页面可加载
  - API 返回正常
  - `_next/static` 资源正常
  - 浏览器控制台无报错
- 现有入口：`npm run smoke:browser`
- 本地一键入口：`npm run smoke:local`
- 使用方式：
  - 本地推荐直接执行 `npm run smoke:local`
  - 若手动执行，则先 `npm run build && npm start`
  - 再执行 `npm run smoke:browser`
  - 如需验证其他环境，设置 `SMOKE_BASE_URL`
  - 若开启登录，补充：
    - `SMOKE_AUTH_EMAIL`
    - `SMOKE_AUTH_PASSWORD`

### 3.2 错误路径可观测性

- [x] 给 LLM 回退增加可观测日志或最小调试信息
- [x] 区分这些失败类型：
  - 服务方无配置
  - 服务方请求失败
  - 服务方返回非 JSON
  - 回退后仍失败
- [x] 避免把所有问题都变成“静默退回规则抽取”，至少要能在服务端定位原因

### 3.3 部署环境收口

- [x] 完成一次真实 Postgres 联调
- [x] 用 Postgres 跑通：
  - 创建案件
  - 读案件列表
  - 发证据
  - 生成预览
  - Final 结案
- 2026-03-25 验证方式：
  - Supabase Postgres `DATABASE_URL` 已接通
  - `drizzle-kit push` 已成功应用 schema
  - 本地生产态 Next 实例已完成真实 API 联调与浏览器冒烟验证
- [ ] 完成一次预览部署验证
  - 登录 -> 新建案件 -> 发首条投诉 -> 继续补碎片 -> summary -> 结果预览
  - 记录控制台报错、鉴权跳转、静态资源状态

### 3.4 工作台拆分准备

- [ ] 在继续追加 `Journey 2` / 导出 / 更多报告工具前，先拆 `components/workspace.tsx`
- [ ] 拆分方向优先保持最小改动：
  - 侧栏 / 案件列表
  - 报告工具区
  - 对话主区
  - 评审 / 摘要卡片
- [ ] 保持状态仍集中在一处，不要过早引入复杂状态库

### 3.5 试用反馈闭环

- [x] 页面内反馈入口
  - 固定可见入口 + 最短分类（看不懂 / 结果不专业 / 报错 / 其他）
- [x] 最小行为埋点
  - 打开工作台 / 新建案件 / 加载种子案例 / 发送证据 / 生成预览 / 生成 final / 报错
- [ ] 种子用户试用组织
  - 最小名单 + 角色划分 + 反馈问题集
  - 固定试用脚本（按用户旅程的多场景）
  - 反馈分类到四类：业务理解 / 对话交互 / 输出质量 / 稳定性
  - 试用脚本（按用户旅程场景，双倍密度）
    - 场景 1：客户投诉邮件（首条完整输入）
      - 输入：整封投诉邮件原文粘贴
      - 期望：AI 识别为高优先级投诉
      - 期望：追问失效位置 / 批次 / 影响范围 / 围堵状态
      - 失败判定：要求用户先选 D / 让用户找按钮继续
    - 场景 2：投诉邮件 + 立即追问客户回复口径
      - 输入：邮件后追加一句“客户 24 小时内要回复，我先怎么说？”
      - 期望：AI 给保守回复建议 + 明确缺口
      - 失败判定：直接生成完整 8D 或回避回复
    - 场景 3：微信碎片补充（零散输入）
      - 输入：3-5 条零散信息，间隔发送
      - 期望：AI 自动归类，不要求选 D
      - 期望：能把碎片合并成清晰事实列表
      - 失败判定：把碎片当成新案件或要求重发
    - 场景 4：碎片输入中夹带问题
      - 输入：事实 + 追问“下一步要先问客户什么”
      - 期望：识别为 evidence + question 混合
      - 期望：给 1-3 个高价值追问
    - 场景 5：会议纪要补录（新信息）
      - 输入：会议纪要 + 新信息
      - 期望：提示“影响前序判断，需回看”
      - 期望：标注受影响阶段
    - 场景 6：新证据推翻旧判断
      - 输入：与已结论冲突的关键信息
      - 期望：解释回看理由
      - 期望：更新当前判断
    - 场景 7：疑似另一单案件（不同客户/型号）
      - 输入：明显不同客户/型号/时间的投诉
      - 期望：触发“疑似新案件”确认
      - 期望：可继续当前或新建
    - 场景 8：相似但可能同案件
      - 输入：同客户不同批次的新问题
      - 期望：AI 说明判断依据并让用户确认
    - 场景 9：已有案件续推（跨会话）
      - 输入：登录后打开历史 case 再补信息
      - 期望：不丢上下文，能继续推进
    - 场景 10：请求总结
      - 输入：直接说“帮我总结一下现在情况”
      - 期望：输出总结卡 + 当前缺口
    - 场景 11：请求行动方案
      - 输入：直接说“先给一个行动方案”
      - 期望：先提示是否已足够，再输出行动方案
    - 场景 12：请求 8D 预览
      - 输入：直接说“给我 8D 预览”
      - 期望：按推荐顺序提示：分析结论 -> 行动方案 -> 8D
      - 失败判定：跳过前置直接出 8D
    - 场景 13：D5 阶段补回 D2 信息
      - 输入：已经推进到 D5 后补 D2 级别事实
      - 期望：AI 标明“影响 D2/D4 需回看”
      - 失败判定：无提示直接推进 D5
    - 场景 14：用户误导输入
      - 输入：用户把“猜测”当成事实写入
      - 期望：AI 标记为待验证而非确认事实
      - 失败判定：直接当已确认写进结论
    - 场景 15：客户要求口径更新
      - 输入：客户说“只要一句话结论”
      - 期望：AI 给简短可用口径 + 提醒风险
      - 失败判定：回避不给结论
    - 场景 16：多事件混合输入
      - 输入：同一条输入包含两个不同客户投诉
      - 期望：AI 拆分并提示新案件确认
      - 失败判定：混成同一案件
    - 场景 17：客户要求承诺时点
      - 输入：客户要求“24 小时内给出根因”
      - 期望：AI 给出保守回应 + 说明当前只能到哪一步
      - 失败判定：承诺无法验证的根因
    - 场景 18：现场处理动作变更
      - 输入：已执行的围堵措施被改动或撤回
      - 期望：AI 提醒风险窗口变化并更新行动建议
      - 失败判定：不更新风险判断
    - 场景 19：外部协作信息补入
      - 输入：供应商或工厂反馈的检查结果
      - 期望：AI 归并进原因链，标注可信度
      - 失败判定：当成客户投诉重复信息
    - 场景 20：用户只想知道“接下来做什么”
      - 输入：一句话“我下一步做什么？”
      - 期望：给 1-3 个可执行动作
      - 失败判定：给泛泛而谈的建议

### 3.10 最简登录与数据隔离

- [x] 本地用户名密码登录
- [x] 未登录无法进入 workspace（登录后进入 case 列表）
- [x] case 数据按用户隔离（ownerUserId）
- [x] 真实部署环境登录验证一轮

### 3.6 对话驱动交互收口

- [ ] 把主交互进一步收口成 `case 管理 + 对话框输入`
- [ ] 页面主路径中不再暴露流程型按钮：
  - `解锁`
  - `复审`
  - 阶段跳转式操作
  - 报告风格 / audience / stage 选择
- [ ] 报告相关动作只保留为对话内轻卡片：
  - `整理当前分析`
  - `整理行动方案`
  - `查看 8D 预览`
- [ ] 默认反馈形态以消息为主，卡片只用于结果整理确认，不再做成工具栏式操作台
- [x] 当前前端已收口：
  - 默认不常驻结果动作卡
  - 仅在 fresh `summary_request` / `decision_signal` 或 fresh 可整理结果出现时，才在消息流里露出动作卡
  - “继续补信息”这类非结果动作不再单独渲染为主按钮

### 3.7 Thinking 状态与时间预期

- [ ] 给每次用户输入增加轻量 `thinking status`
- [ ] 统一展示：
  - 开始时间
  - 处理步骤摘要
  - 预计耗时
  - 完成时间 / 实际耗时
- [ ] 不暴露原始思维链，只展示高层处理步骤
- [ ] 至少覆盖四种状态：
  - `处理新信息`
  - `回看前序判断`
  - `整理当前情况`
  - `准备结果预览`

### 3.8 碎片输入与跨阶段回看

- [ ] 服务端把每条用户输入统一识别为多意图输入，而不是单一路径
- [ ] 当前至少显式支持这些意图：
  - `evidence`
  - `question`
  - `summary_request`
  - `correction`
  - `decision_signal`
- [ ] 每条输入都要做阶段映射，输出：
  - `primaryStage`
  - `relatedStages`
  - `impactedStages`
- [ ] 当用户在后段补入前段信息时，系统自动回看并在消息里解释影响：
  - 例如当前已在 `D5`，但新信息影响 `D2 / D4`
- [ ] 不要求用户主动声明“这条信息属于哪个 D”，由系统自行判断
- [ ] 不要求用户手动点击“复审”，由 AI 在消息中说明需要回看的段落

### 3.9 质量工程师真实提问 benchmark

- [ ] 建一套按真实现场语言组织的用户提问集，不按教材式 8D 问法设计
- 当前权威资产入口：
  - [`docs/journeys/README.md`](/Users/jilanfang/ai-quality/docs/journeys/README.md)
  - [`docs/journeys/fireline-hybrid-user-journey-ledger.md`](/Users/jilanfang/ai-quality/docs/journeys/fireline-hybrid-user-journey-ledger.md)
  - [`docs/journeys/fireline-structured-scenario.schema.json`](/Users/jilanfang/ai-quality/docs/journeys/fireline-structured-scenario.schema.json)
  - [`docs/journeys/fireline-structured-scenarios.sample.json`](/Users/jilanfang/ai-quality/docs/journeys/fireline-structured-scenarios.sample.json)
- [ ] Benchmark 至少覆盖：
  - 每个 `D1-D8` 的常见卡点提问
  - 碎片输入
  - 总结请求
  - 新证据推翻旧判断
  - “现在能不能先给说法 / 给客户回复”
- [ ] 模型评测继续区分两类能力：
  - 渐进办案能力
  - 最终正式成稿能力
- [ ] 当前主测模型保持：
  - `deepseek-v3.2`
  - `qwen3.5-122b-a10b`
  - `ark-code-latest`

### 3.10 证据驱动 8D 工作流收口

> 2026-04-03 `/plan-eng-review` 结论：方向成立，不推翻“用户持续贴证据，系统持续更新 8D 草稿”的主路径；但当前实现仍有几处旧状态机残留，必须在继续扩展前收口。

- [ ] 把 `workflowState` 定为工作台唯一焦点真相源
  - 中栏、右栏、顶部状态、composer 提示统一读 `workflowState.focusArea`
  - `guidedThinking` 改为从 `focusArea` 派生，不再和 `currentStage` 双轨并存
- [ ] 把“聊天消息持久化”和“证据写入 case 状态”解耦
  - `question_only` / `summary_request` 也要保留用户消息
  - 但这类输入默认不改事实层、阶段层
  - 避免 IM 消息流里只看到 AI 回复、看不到用户刚刚问了什么
- [ ] 把 `D4` 收口成结构化派生，不再靠展示文案反解析状态
  - 内部统一产出：`changePoint / 发生原因 / 流出原因 / 支持证据 / 待验证项`
  - `ready` 判断、缺口提示、右栏预览、确认按钮都读取同一结构
- [ ] 修正 `D4 stale` 展示优先级
  - 一旦新证据推翻旧判断，右栏优先展示最新待复审稿
  - 不再优先显示旧 `confirmedContent`
- [ ] 调整 final 8D readiness 规则，彻底切到“证据成熟度”而不是“逐阶段锁死”
  - `final` 的主门槛是：
    - `D4` 已确认
    - `D5-D7` 有可用内容
    - `D6` 有真实验证信息，不是模板占位
    - 无 `stale/impacted`
  - 不再默认要求 `D5-D8` 全部 `locked`
- [ ] 修正 `D1` 自动草稿的展示源
  - 优先显示用户名或稳定 display name，不显示内部 `ownerUserId`
  - 统一角色推断字段，不再混用 `lot / batch`

### 3.11 证据驱动 8D 回归测试补齐

- [ ] API 回归
  - `question_only` 输入后，消息流必须同时保存用户消息和 AI 回复
  - `summary_request`、`question`、`evidence` 混合输入都要验证消息顺序完整
- [ ] Workspace 回归
  - 纯提问后消息流必须同时出现“你”和“AI 助手”
  - `D4 stale` 后右栏必须显示最新待复审内容，而不是旧确认内容
  - 中栏、右栏、顶部状态必须基于同一焦点，不允许出现 split-brain UI
- [ ] Report builder 回归
  - 增加正例：`D5-D8` 未全 locked，但证据成熟时仍允许 final 8D
  - 增加反例：`D6` 仍是模板占位时，必须阻断 final 8D
  - 逐步移除旧测试里“all stages are locked 才能 final”的基线
## 4. 可以随后再做

这些是后续值得做，但不该阻塞 MVP。

### 4.1 完整 LLM 平台化

- [ ] 真正落地“任务统一入口 + 模型路由 + 服务适配层”
- [ ] 扩展到：
  - `copilot`
  - `report`
  - 更细粒度任务类型

### 4.2 更细的领域拆分

- [ ] 视 Journey 2 和更多输出模式推进情况，再决定是否拆：
  - `workflow-engine`
  - `report-builder`
  - 专家审稿逻辑
- [ ] 当前不要为了“看起来更优雅”先拆

### 4.3 更重的平台能力

- [ ] 登录 / 权限
- [ ] 多人协作
- [ ] 审批流
- [ ] 知识库 / RAG
- [ ] 队列 / 异步编排
- [ ] 微服务化

## 5. 不在当前 MVP 范围

- 不做多租户
- 不做复杂组织权限
- 不做独立 prompt 管理后台
- 不做自建模型平台
- 不做为了解决未来问题而提前引入的基础设施

## 6. 建议推进顺序

```text
当前
  -> 部署边界写死
  -> LLM 接口边界写死
  -> 离线原型回灌账本建立
  -> 运行态污染规则固化

对外试用前
  -> 浏览器冒烟验证
  -> 错误路径可观测性
  -> Postgres 预览部署验证
  -> 工作台组件最小拆分

后续
  -> 完整 LLM 平台化
  -> 进一步领域拆分
  -> 登录 / 协作 / 知识库等平台能力
```

## 7. 一句话执行原则

当前阶段的正确策略不是“重做架构”，而是：

**保持单体、保持显式、保持测试护栏，在不扩大系统复杂度的前提下把 MVP 从本机演示推到可外部试用。**
