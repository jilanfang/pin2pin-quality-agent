# 设计系统 — Pin2pin Fireline

## 产品背景

- **这是什么：** 一个面向电子制造场景的 AI 原生失效分析工作台，帮助用户把零碎现场信息整理成结构化 case，推进异常分析，并自然沉淀出分析结论、行动方案和 8D。
- **面向谁：** 1 到 5 年经验的年轻质量工程师。其它角色不是当前主模型，不作为主界面和主文案的默认假设。
- **行业场景：** 中国电子制造、芯片供应链、质量分析与失效分析工具。
- **项目类型：** 以 Web 应用为主的专业工作台，附带正式报告输出能力。

## 视觉方向

- **方向：** 冷静工程侦查（Industrial Investigation Editorial）
- **装饰强度：** 有意识，但不过度
- **气质：** 不做泛 SaaS 的“轻快效率工具”，而是做成“资深工程师带你查案”的工作环境。它应该看起来冷静、可信、专业，带一点案情推演和证据收束的紧张感，但不能戏剧化到像游戏 UI。
- **当前布局语法：** 会话主舞台 + 抽屉辅助区（Conversation-First Investigation Desk）
- **结构判断：** 屏幕中心永远优先给消息流，左侧案件、右侧报告、顶部状态都必须缩小、折叠或浮层化，不能长期挤占主对话区。
- **参考方向：** 当前版本未做外部竞品视觉采样，先基于产品定位与现有实现沉淀第一版系统；后续如要做更强品牌化，可补电子仪器、工业软件、编辑式数据产品的视觉研究。

## 核心设计判断

- 这不是“填表工具”，而是“失效分析推进工作台”。
- 用户的核心感受不应是“我在操作模块”，而应是“AI 正在带我推进案件”。
- 当前产品已经从“混合工作台”收口到“会话优先工作台”。
- 分析结论、行动方案、8D 都属于分析推进后的结果产物，不应反过来驱动用户操作主界面。
- 报告预览、案件列表、阶段辅助信息都属于配角，只在需要时以抽屉、轻浮层、内联卡片的形式出现。
- 页面必须始终回答三个问题：
  - 现在是什么案件、进展到哪里。
  - 当前唯一最重要的下一步是什么。
  - 现在离“可执行结论 / 行动方案 / 正式 8D”还有多远。

## 视觉人格

### 稳妥做法

- 保持中文工程软件的可信、清晰和高可读性，不用炫技型色彩和过重装饰。
- 保持冷色中性背景、清楚的卡片层级、明确的信息分栏。
- 保持“文本优先”，避免大量图形控件和花哨可视化。

### 有意识承担的风险

- **风险 1：把产品气质从“通用 SaaS”拉向“工程案件桌面”**
  - 通过更明确的证据、阶段、状态语义和更克制的品牌语言实现。
  - 收益：更有记忆点，更贴近 8D 的真实工作心智。
  - 代价：不再像常规 SaaS 那样“人人都熟悉”。
- **风险 2：让报告标题和关键文档块有一点“正式文书感”**
  - 工作台仍以无衬线为主，但正式报告标题可以更有文档权威感。
  - 收益：输出更像可交付成果，不像内部草稿。
  - 代价：如果控制不好，会显得老气。
- **风险 3：弱化按钮中心，强化“AI 当前建议”中心**
  - 用户视线优先落在阶段目标、追问和证据空缺，而不是先找按钮。
  - 收益：更符合 AI 原生工作台的心智。
  - 代价：会要求更强的信息编排能力。

## 字体系统

- **标题字体：** `Noto Serif SC`, `Source Han Serif SC`, `Songti SC`, serif
  - 用于正式报告标题、阶段标题中的重要节点、需要传达“结论/文档权威”的位置。
  - 不是全站主字体，只在少数高价值位置使用。
- **正文字体：** `PingFang SC`, `Hiragino Sans GB`, `Microsoft YaHei`, sans-serif
  - 当前最务实的首版方案。
  - 中文显示稳定、系统可用、部署成本低，适合当前演示版。
- **界面标签：** 同正文字体
  - 标签、面板标题、引导文案全部与正文共用，靠字重和间距拉层级，不靠花哨字体。
- **数据与表格：** `JetBrains Mono`, `IBM Plex Mono`, `SFMono-Regular`, monospace
  - 仅用于批次、工单、料号、版本号、状态码等拉丁字符与数字串。
  - 不用于大段中文正文。
- **代码与编号：** `JetBrains Mono`, `IBM Plex Mono`, monospace
- **加载策略：** 首版工作台优先系统中文字体，保证部署稳定；正式报告标题如需引入 `Noto Serif SC`，建议后续按需引入或本地化，不要在当前阶段为了字体增加不必要复杂度。
- **字号层级：**
  - 主标题：`40px / 1.1`
  - 页面标题：`30px / 1.2`
  - 分区标题：`22px / 1.25`
  - 卡片标题：`16px / 1.4`
  - 正文：`13px / 1.65`
  - 密集元信息 / 标签：`11-12px / 1.45`

## 色彩系统

- **方法：** 克制
- **主色：** `#1949CB`
  - 用于主流程状态、高优先级操作、当前阶段焦点。
  - 是“系统正在推进”的颜色，不是装饰色。
- **辅助色：** `#11745B`
  - 用于“验证通过、闭环、确认有效”这类偏成熟结果。
  - 不与 primary 争主导权。
- **中性色：** 冷灰蓝体系
  - `#F5F7FB` 页面背景高亮面
  - `#EDF1F7` 页面主背景
  - `#D7DDEA` 分隔线
  - `#647287` 次级文字
  - `#18202C` 主文字
- **语义色：**
  - 成功 `#11745B`
  - 警示 `#A86D16`
  - 错误 `#B04444`
  - 信息 `#1949CB`
- **深色模式：** 当前阶段不做。先把浅色工作台与报告风格做扎实，再考虑暗色模式。

## 界面表面与状态

- 页面背景不是纯平白，而是浅冷底 + 弱径向光斑，形成“桌面空间感”。
- 卡片表面使用高透明白或实白，优先强调内容而不是容器。
- 当前阶段卡片应有明确高亮边框与轻微内发光。
- `受影响` 状态必须强于普通警示状态，要让用户一眼看出“前面结论被新证据击中了”。
- 已确认、工作稿、待验证三种状态不能只靠文字区分，后续要加可识别的色条/标记体系。

## 间距系统

- **基础单位：** 8px
- **密度：** 紧凑但不拥挤
- **间距层级：** `2xs(4) xs(8) sm(12) md(16) lg(24) xl(32) 2xl(48) 3xl(64)`
- 规则：
  - 工作台卡片内部优先用 `10-14px` 的垂直节奏。
  - 主会话区之外的固定结构默认再缩一档，优先使用 `8-12px` 间距。
  - 不要把每一层都做成一样的间距，重点模块应更“贵”。

## 布局系统

- **方式：** 会话主舞台 + 抽屉辅助区
- **栅格：**
  - 桌面端：窄左 rail + 主会话区；案件抽屉与报告抽屉按需覆盖，不推挤主聊天区
  - 主内容区内部：单列消息流为主，辅助信息以内联卡、微 chips、只读时间线嵌入
  - 移动端：单列堆叠，但保留“当前案件 / 当前对话”优先于其它辅助信息的顺序
- **最大内容宽度：** 工作台模式下不做过窄限制；正式报告页建议 `960px`
- **圆角：**
  - sm: `12px`
  - md: `16px`
  - lg: `20px`
  - xl: `28px`
  - full: `9999px`
- **新增规则：**
  - 固定区域必须能“缩小、折叠、浮层化”，不能长期挤占主对话区。
  - 右侧报告预览默认关闭，作为 overlay drawer 打开。
  - 左侧案件区默认只保留窄 rail，详细列表通过抽屉展开。

## 交互模型

- 当前页面的固定骨架要遵循以下优先级：
  1. 消息流
  2. AI 当前目标与下一条追问
  3. 浮动输入 dock
  4. 内联阶段时间线与阶段说明
  5. 按需展开的案件抽屉与报告抽屉
- 交互原则：
  - 少按钮，但关键动作必须清晰。
  - 主交互尽量靠对话和消息卡推进，而不是靠工具栏和整排按钮。
  - 不让用户思考“下一步点哪里”，而是让页面自己说出“现在先补什么”。
  - 用户提交证据后，页面要明显反馈“案件发生了什么变化”。
  - 新证据影响旧判断时，由 AI 在消息里明确说明“需要回看哪里”，而不是要求用户先找“复审”按钮。
  - 用户主操作默认只有两类：管理 case、在对话框里说话。

## 动效策略

- **方式：** 极简、功能性优先
- **缓动：** 进入 `ease-out`，退出 `ease-in`，移动 `ease-in-out`
- **时长：**
  - 微动 `80-120ms`
  - 短 `160-220ms`
  - 中 `240-320ms`
  - 长 `360-480ms`
- 动画只服务于三件事：
  - 焦点切换
  - 新证据导致状态变化
  - 预览与报告生成反馈
- 不做通用漂浮动效，不做装饰性持续动画。

## 工作台指导

### 当前优点

- 左侧案件 / 中间工作区 / 右侧辅助信息的主结构是合理的。
- 空态足够清楚，新用户可以理解从哪里开始。
- “文本 + 正式报告 HTML”双预览方向是对的。

### 当前问题

- 当前工作台一度过于像“把状态机摊给用户看”，而不是 AI 主导的调查过程。
- 固定右侧预览和顶部工具条会挤压消息流，削弱会话主舞台。
- 阶段卡片文字过长时，可读性迅速下降。
- 工作台视觉语言和报告语言尚未完全分层，导致报告还带内部过程感。

### 下一轮视觉重点

1. 强化“当前阶段 / 当前目标 / 当前缺口”的主叙事，并把它们并入消息流。
2. 压缩次要模块视觉权重，减少固定 panel 和孤立工具条。
3. 为阶段状态建立更强的视觉语义，而不是只靠文字标签。
4. 把正式报告做成“可打印文书”，但在工作台里默认只通过抽屉按需查看。

## 结果产物设计

- **目标:** 系统固定沉淀三类结果产物：
  - `分析结论`
  - `行动方案`
  - `8D 报告`
- `8D` 是分析完成后的正式文档，不是用户一开始就要选的模式。
- **目标:** 正式 8D 必须像“可以交付”的工程文书，而不是工作过程截面。
- **保留:**
  - 简洁头部
  - 基础摘要卡
  - 清晰章节顺序
- **必须去掉或转译的内容:**
  - `verified / ready / draft` 这种内部成熟度英文词
  - “建议 / 已确认上下文 / 当前补充”这类过程性拼接表达
  - 重复罗列前文上下文导致的冗长正文
- **首版 8D 风格:** 中文工程报告风格，干净、正式、可打印。
- 不再把 `对客 / 对内 / 文风选择` 暴露给用户作为前置操作。
- 若系统需要控制表达边界，应由 AI 在文案里解释“当前只整理已确认事实”“当前还不写未验证根因”，而不是让用户选抽象风格。

## 内容语气

- 工作台语气：冷静、协作、引导式，不居高临下。
- 分析结论 / 行动方案语气：工程化、直接、可执行，不装腔。
- 8D 语气：专业、直接、结构化，不做空泛礼貌堆砌。
- AI 不应表现成“万能判断者”，而应表现成“高质量案件推进伙伴”。

## 实现说明

- 当前颜色 token 已经存在于 [app/globals.css](/Users/jilanfang/ai-quality/app/globals.css)。
- 当前大部分工作台样式还内嵌在 [components/workspace.tsx](/Users/jilanfang/ai-quality/components/workspace.tsx)。
- 下一轮前端重构时，应把以下内容逐步抽成设计资产：
  - 颜色 token
  - 字体与字号 token
  - 状态 token
  - 通用面板与按钮样式
  - 报告页单独样式体系

## 决策记录

| 日期 | 决策 | 原因 |
|------|------|------|
| 2026-03-23 | 第一版设计系统确立为“冷静工程侦查”方向 | 比通用 SaaS 更贴近 8D 真实工作心智，也更符合“AI 带你查案”的产品愿景 |
| 2026-03-23 | 工作台首版继续使用系统中文无衬线为主 | 保证当前演示版的部署稳定性与可读性，避免为字体增加复杂度 |
| 2026-03-23 | 正式报告与工作台视觉明确分层 | 报告需要更强文书感，不能继续暴露内部过程状态 |
| 2026-03-23 | 暂不做暗色模式 | 先把浅色工作台和报告交付质量做扎实，避免分散设计与实现资源 |
